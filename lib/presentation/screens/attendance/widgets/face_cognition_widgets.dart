import 'package:flutter/material.dart';

class FaceCognitionWidgets extends StatelessWidget {
  const FaceCognitionWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          const Text('Face Cognition Widgets'),
        ],
      ),
    );
  }
}
